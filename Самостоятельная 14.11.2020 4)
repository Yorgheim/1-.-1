#include <iostream>
#include <iomanip>
using namespace std;

int main()
{
	int a, b, c, d, n;
	cin >> n >> a >> b >> c >> d;
	for (int i = 0; i < a; i++)
		cout << i << " ";
	for (int i = b; i >= a; i--)
		cout << i << " ";
	for (int i = b + 1; i < c; i++)
		cout << i << " ";
	for (int i = d; i >= c; i--)
		cout << i << " ";
	for (int i = d + 1; i <= n; i++)
		cout << i << " ";
}
